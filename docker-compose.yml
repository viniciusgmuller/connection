services:
  postgres:
    image: postgres:16-alpine
    container_name: connection-postgres
    environment:
      POSTGRES_USER: connection
      POSTGRES_PASSWORD: connection
      POSTGRES_DB: connection_cms
    ports:
      - "5435:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
